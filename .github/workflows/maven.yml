name: Test Automation Workflow

on:
  schedule:
  - cron: 00 18 * * *
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: Set up JDK 11
      uses: actions/setup-java@v4
      with:
        java-version: '11'
        distribution: 'temurin'
        cache: maven
    - name: Running Test Automation  Framework with maven
      run: mvn test -X -Dbrowser=chrome -DisLambdaTest=false -DisHeadless=true

    - name: Upload a Test logs 
      if: always()
      uses: actions/upload-artifact@v7.0.1
      with:
        name: Test Logs
        path: logs/

    - name: Upload a Test Reports 
      if: always()
      uses: actions/upload-artifact@v7.0.1
      with:
        name: html.report
        path: report.html

    - name: Test Execution status
      if: always()
      run: echo "Test Exexution Completed"

    
    
